<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
</head>
<body>
<section class="hero is-fullheight is-white">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-centered">
                <div class="column is-4-desktop">
                    <div class="">
                        <div class="card-content">
                            <figure class="image is-64x64" style="margin: 0 auto;">
                                <img class="is-rounded" src="/img/chalom-logo.jpg">
                            </figure>
                            <h1 class="has-text-centered is-size-4 pt-4 pb-4 pl-4 pr-4">Reset Code Verification</h1>
                            {% if errors|length > 0 %}
                                <div class="notification is-danger">
                                    {% for error in errors %}
                                        <p>{{ error }}</p>
                                    {% endfor %}
                                </div>
                            {% endif %}

                            {% if success|length > 0 %}
                                <div class="notification is-success">
                                    {% for succ in success %}
                                        <p>{{ succ }}</p>
                                    {% endfor %}
                                </div>
                            {% endif %}
                            <p class="has-text-centered is-size-7 pt-3 pb-3">Enter code sent to : <span class="has-text-info">{{ phone }}</span> </p>
                            <form class=" pl-4 pr-4" action="" method="post">
                                <div class="field ">
                                    <label class="">Code</label>
                                    <div class="control">
                                        <input class="input is-medium  is-link " id="code" name="code" type="text" placeholder="Enter code" required>
                                    </div>
                                </div>
                                <div class="field has-text-centered mt-5">
                                    <button class="button is-info is-medium is-fullwidth">Verify</button>
                                </div>
                                <p class="mt-4  has-text-centered is-size-7">
                                    All is set?  <a class="has-text-info" href="/">Sign In</a>
                                </p>
                                <hr>
                            </form>
                            {#                            <hr>#}
                            <p class="has-text-centered">All rights reserved &copy; {{ "now"|date("Y") }}</p>
                            <p class="has-text-centered is-size-7">Chalom Investments Ltd</p>
                            <p class="has-text-centered is-size-7"> <strong>Powered By:</strong> <a class="has-text-link" href="https://www.fortresshubtechnologies.com">Fortress Hub Technologies Limited</a> </p>
                        </div>

                    </div>

                </div>
            </div>
        </div>
    </div>
</section>
</body>
</html>